# Pinned exactly so the container, CI and a local checkout resolve identically.
# Versions chosen to clear known CVEs; see docs/SECURITY.md before downgrading.
discord.py==2.7.1
aiohttp==3.14.3
aiosqlite==0.22.1
feedparser==6.0.14
python-dotenv==1.2.3
tzdata==2026.3
